<h1>Transaction</h1>
<table class="table table-condensed">
  <tr>
    <th>Date</th>
    <td><%= @income.trans_date %></td>
  </tr>
  <tr>
    <th>Description</th>
    <td><%= @income.description %></td>
  </tr>
  <tr>
    <th>Amount</th>
    <td><%= @income.amount %></td>
  </tr>
  <tr>
    <th>Type</th>
    <td><%= @income.type %></td>
  </tr>
  <tr>
    <th>Category</th>
    <td><%= @income.category.name %></td>
  </tr>
</table>
<%= link_to 'Back', sti_transaction_path("Transaction") %> | 
<%= link_to 'Edit', sti_transaction_path(@transaction.type, @transaction, :edit) %> | 
<%= link_to 'Destroy', sti_transaction_path(@transaction.type, @transaction), method: :delete, data: { confirm: 'Are you sure?' } %>
<br><br>